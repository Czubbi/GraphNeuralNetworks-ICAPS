prints the causal graph of the given planning task <problem.pddl> to
src/<problem.pddl.png> using the dot tool.

SCCs in the causal graph are painted in the same color.

Usage is as for standard FD:

./build_all
./print_cg [domain_file] problem_file
